/**
 * App header component.
 * Per D-07: single-column stacked layout, header at top.
 */
export function Header() {
  return (
    <header className="mb-6">
      <h1 className="text-2xl font-bold text-gray-900">Caffeine Tracker</h1>
    </header>
  );
}
